{
  "name": "three-little-prince",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 1001 --open=/",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 1011 --open=/"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "dependencies": {
    "three": "^0.149.0"
  }
}
